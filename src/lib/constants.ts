// src/lib/constants.ts
export const API_BASE_URL = 'http://localhost:4000/api';

// Otras constantes que podrías necesitar más adelante
export const DEFAULT_PAGE_SIZE = 10;
export const DEFAULT_PAGE = 1;

// Constantes para filtros o estados
export const STATUS_ACTIVE = true;
export const STATUS_INACTIVE = false;

// Puedes agregar más según necesites (ej. timeouts, claves de API, etc.)